<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_voiceml_number_tree" model="ir.ui.view">
        <field name="name">connect.voiceml.number.tree</field>
        <field name="model">connect.voiceml.number</field>
        <field name="arch" type="xml">
            <list>
                <field name="phone_number"/>
                <field name="friendly_name"/>
                <field name="destination"/>
                <field name="user"/>
                <field name="callflow"/>
                <field name="application"/>
                <field name="is_ignored"/>
            </list>
        </field>
    </record>

    <record id="view_voiceml_number_form" model="ir.ui.view">
        <field name="name">connect.voiceml.number.form</field>
        <field name="model">connect.voiceml.number</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group string="Number">
                            <field name="phone_number"/>
                            <field name="friendly_name"/>
                            <field name="is_ignored"/>
                        </group>
                        <group string="Routing">
                            <field name="destination"/>
                            <field name="user" invisible="destination != 'user'"/>
                            <field name="callflow" invisible="destination != 'callflow'"/>
                            <field name="application"
                                   invisible="destination != 'application'"
                                   required="destination == 'application'"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_voiceml_number" model="ir.actions.act_window">
        <field name="name">Numbers</field>
        <field name="res_model">connect.voiceml.number</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_voiceml_numbers"
              name="Numbers"
              parent="menu_connect_voiceml"
              action="action_voiceml_number"
              sequence="10"/>
</odoo>
