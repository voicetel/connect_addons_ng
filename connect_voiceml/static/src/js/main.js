/** @odoo-module **/
// VoiceML phone bootstrap.
//
// TODO(connect_voiceml): see utils.js for the JsSIP wiring plan. This file
// is intentionally a placeholder so the module's asset bundle loads cleanly;
// the softphone component (components/phone/phone.js) will own the UA once
// JsSIP is vendored.
import { normalizeNumber } from './utils.js';

export { normalizeNumber };
